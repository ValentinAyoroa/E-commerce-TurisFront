<!DOCTYPE html>
<html lang="en">

<!--head-->
<%- include("./partials/head.ejs") %>
    <link rel="stylesheet" href="/css/forms-styles.css">

    <body>
        <!--header-->
        <%- include("./partials/header.ejs") %>
            <div class="register">
                <div class="register-top">
                    <h2 class="copy-register">Hola! Para seguir ingresa tus datos</h2>
                    <form action="/users/login" method="post" class="form-register">
                        <div>
                            <input type="email" name="email" id="email" value= '<%= locals.old && !locals.errors.email ? old.email : "" %>' placeholder="Nombre de usuario o correo">

                            <% if(locals.errors && locals.errors.email){ %>
                                <p class="errors">
                                    <%= locals.errors.email.msg %>
                                </p>
                                <% } %>
                        </div>
                        <div>
                            <input type="password" name="password" id="password" placeholder="Ingresá tu contraseña">

                            <% if(locals.errors && locals.errors.password){ %>
                                <p class="errors">
                                    <%= locals.errors.password.msg %>
                                </p>
                                <% } %>
                        </div>
                        <div class="mb-3 form-check">
                            <input type="checkbox" name="remember" class="form-check-input" id="exampleCheck1">
                            <label class="form-check-label" for="exampleCheck1">Recordar usuario</label>
                        </div>

                        <% if(locals.errorLogin){ %>
                            <p class="errors">
                                <%= errorLogin %>
                            </p>
                            <% } %>

                                <div class="button">
                                    <button type="submit">Ingresar</button>
                                </div>
                    </form>
                    <p class="copy-login">¿No tienes cuenta? Crea una <a class="login-button" href="./register">aqui</a>
                    </p>
                    <p class="login-help">Necesito ayuda para ingresar</p>
                </div>
                <div class="register-bottom">
                    <div class="register" href="#">
                        <p>Ingresar con Google</p>
                        <img src="/images/marcas/google.png" class="img-logo-app">
                    </div>
                    <div class="register" href="#">
                        <p>Ingresar con Facebook</p>
                        <img src="/images/marcas/facebook.png" class="img-logo-app">
                    </div>
                    <div class="register" href="#">
                        <p>Ingresar con Apple</p>
                        <img src="/images/marcas/apple.png" class="img-logo-app">
                    </div>
                </div>
            </div>
            <!--footer-->
            <%- include("./partials/footer.ejs") %>
    </body>

</html>